name: windows-docker
on: push
jobs:
  build:
    name: Hello world action
    runs-on: windows-latest
    steps:
      - name: checking pwd
        shell: cmd
        run: echo $PWD
      - uses: actions/checkout@v2
      - name: checking pwd again
        shell: cmd
        run: echo $PWD
        
